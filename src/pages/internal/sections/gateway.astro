---
import VisualPage from '@root/pages/internal/_VisualPage.astro';
import Variant from '@root/pages/internal/_Variant.astro';
import GatewayDiagram from '@components/Landing/GatewayDiagram.astro';
import GatewayHub from '@components/Landing/GatewayHub.astro';
import { kv } from '@root/pages/internal/_key-visuals';

/**
 * IoT Gateway. Directions newest first: `GatewayHub` redraws it in the Connect and Platform visuals'
 * vocabulary; `GatewayDiagram` is the circuit board the ecosystem card ships with.
 *
 * Its own route, so opening it compiles and renders this visual and nothing else. `_VisualPage`
 * carries what every visual's page shares: the strip of links between them and the promote/retire
 * control.
 */
---

<VisualPage visual="gateway">
	<Variant
		visual={kv('gateway')}
		component={GatewayHub}
		name="Hub"
		note="candidate · ConnectHub's fan into a hub node, MQTT as one more chip on the line to ThingsBoard; native 422u, floor 16u (R 26.4)"
	/>
	<Variant
		visual={kv('gateway')}
		component={GatewayDiagram}
		name="Diagram"
		note="existing · one root svg on a 720 viewBox, not a grid on the design unit — it scales as a piece and never reflows"
	/>
</VisualPage>
