---
import VisualPage from '@root/pages/internal/_VisualPage.astro';
import FeatureRow from '@root/pages/internal/_FeatureRow.astro';
import NormalizeFlow from '@components/Landing/NormalizeFlow.astro';
import NormalizeSeries from '@components/Landing/NormalizeSeries.astro';
import { kv } from '@root/pages/internal/_key-visuals';

/**
 * Normalize — the transform flow.
 *
 * Its own route, so opening it compiles and renders this visual and nothing else. `_VisualPage`
 * carries what every visual's page shares: the strip of links between them, the retire control
 * and the stage chrome.
 */
---

<VisualPage visual="normalize">
	{
		/* First half of the design's "Turn IoT data into action" section. The second half — a rule
	    chain raising an alarm and notifying — is a separate argument and will be its own visual,
	    so the copy in this row still covers both and will need splitting with it. */
	}
	<FeatureRow visual={kv('normalize')}>
		<NormalizeSeries />
	</FeatureRow>

	{
		/* The candidate goes first, in the row and at both probe widths, and the incumbent follows.
	    Put it second and the incumbent quietly stays the incumbent. */
	}
	<figure class="stage" data-w="full">
		<figcaption class="stage__cap">
			<span class="stage__name">NormalizeSeries · 673px</span>
			<span class="stage__note">candidate · the mark for the pill, a line for the repeated key</span>
		</figcaption>
		<div class="stage__frame stage__frame--plain">
			<div class="probe" style="--probe: 673px">
				<NormalizeSeries />
			</div>
		</div>
	</figure>

	<figure class="stage" data-w="full">
		<figcaption class="stage__cap">
			<span class="stage__name">NormalizeSeries · 335px</span>
			<span class="stage__note">candidate · phone</span>
		</figcaption>
		<div class="stage__frame stage__frame--plain">
			<div class="probe" style="--probe: 335px">
				<NormalizeSeries />
			</div>
		</div>
	</figure>

	{/* The same row the candidate is answering, kept below it so the comparison is one scroll. */}
	<figure class="stage" data-w="full">
		<figcaption class="stage__cap">
			<span class="stage__name">NormalizeFlow · in the row</span>
			<span class="stage__note">incumbent · what the home preview renders</span>
		</figcaption>
		<div class="stage__frame stage__frame--plain">
			<div class="probe" style="--probe: 525px">
				<NormalizeFlow />
			</div>
		</div>
	</figure>

	<figure class="stage" data-w="full">
		<figcaption class="stage__cap">
			<span class="stage__name">NormalizeFlow · 673px</span>
			<span class="stage__note">incumbent · the width the feature row gives it</span>
		</figcaption>
		<div class="stage__frame stage__frame--plain">
			<div class="probe" style="--probe: 673px">
				<NormalizeFlow />
			</div>
		</div>
	</figure>

	{/* The same composition at the phone width, to check the curves survive the scale-down. */}
	<figure class="stage" data-w="full">
		<figcaption class="stage__cap">
			<span class="stage__name">NormalizeFlow · 335px</span>
			<span class="stage__note">incumbent · phone</span>
		</figcaption>
		<div class="stage__frame stage__frame--plain">
			<div class="probe" style="--probe: 335px">
				<NormalizeFlow />
			</div>
		</div>
	</figure>
</VisualPage>
